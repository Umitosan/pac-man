/*jshint esversion: 6 */

function Ghost() {

  this.init = function() {
    // init
  };
  this.draw = function() {
    // draw slice of image:   drawImage(image, sx, sy, sWidth, sHeight, dx, dy, dWidth, dHeight)
  };
  this.update = function() {
    // update positions
  };
}
